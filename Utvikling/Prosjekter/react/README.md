# Kokebok-app med React + Vite

Når du har lastet ned koden, kjør `npm install`. Kjør deretter `npm run dev` for å kjøre nettsiden. Ctrl + klikk på lenken som kommer opp i terminalen for å åpne nettsiden.
